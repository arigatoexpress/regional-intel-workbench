repos:
  - repo: local
    hooks:
      - id: unittest
        name: unittest
        entry: python -m unittest discover -s tests -v
        language: system
        pass_filenames: false
        files: ^(app|tests)/.*\.py$
